[project]
name = "toknizer-viz"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.128.5",
    "pydantic>=2.12.5",
    "tokenizers>=0.22.2",
    "transformers>=5.1.0",
    "uvicorn>=0.40.0",
]
